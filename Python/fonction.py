#Fonction max() permet d'afficher la valeur la plus grande.
maximum = max(85,69,102,102.3)
print (maximum)

#Fonction min() permet d'afficher la plus petite valeur.
minimum = min(85,69,102,102.3)
print (minimum)

####################################################################################################################################

str()    #Transforme en chaîne de caractère.

####################################################################################################################################

chaine = "Python"                   
longueur = len(chaine)            #len() nous renvoie le nombre de lettre dans la variable.
print(longueur)  # 6

####################################################################################################################################

.upper() #Met en majuscule.
.lower() #Met en minuscule.

####################################################################################################################################

"""The abs() function returns the absolute value of the number it takes as an argument—that is, that number’s distance from 0 on
 an imagined number line. For instance, 3 and -3 both have the same absolute value: 3."""

absolute = abs(-42)
print (absolute)      #Le résulta sera donc 42.

####################################################################################################################################

"""Finally, the type() function returns the type of the data it receives as an argument."""

print(type(42))
print(type(4.2))
print(type('spam'))
=
<type 'int'>
<type 'float'>
<type 'str'>

#########################################################################################################

print("Hello \n world ")   #Permet de mettre le message en 2 partie à la ligne.